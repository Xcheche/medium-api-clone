2025-06-13:
 mkdir medium-api-clone && cd medium-api-clone.
 mkdir src && cd src
 npx gitignore python
 mkdir  requirements folder - base.txt,production.txt,local.txt
 python -m ven venv


-r base.txt ( we did this inside local.txt to connect to base.txt)
pip install -r requirements/local.txt 


startproject config


==Generate good django secret key==
python -c "import secrets; print(secrets.token_urlsafe(7))"

==Logger===
===To confirm local.yml==
docker compose -f local.yml config

docker compose -f local.yml up --build -d --remove-orphans

=====To build=============
docker compose -f local.yml up --build
docker compose -f local.yml up -d --remove-orphans
========To run backup===============
docker compose -f local.yml exec postgres backup

====To see list of backups==========
docker compose -f local.yml exec postgres backups



========To restore backup===============
docker compose -f local.yml exec postgres restore <backup-filename>

